class ApiUrls {
  static const baseURL = "http://localhost:anyport";
  static const register = "${baseURL}auth";
  static const userProfile = '${baseURL}users/profile';
  static const login = '${baseURL}auth/login';
}
